SDKCONFIG涉及方方面面，所以并不是需要全部学习，本文只涉及尽可能会被接触的
任务 （线程）
开启蓝牙升级，升级中看门狗超时，将CONFIG_FREERTOS_HZ设置为1000
名称
定义
说明
使用(如有)
CONFIG_FREERTOS_HZ
CONFIG_FREERTOS_HZ=100

1秒分100份，每一份是一个Tick（10ms为1tick）
vTaskDelay(1)
Bootloader
名称
定义
说明
使用(如有)
CONFIG_BOOTLOADER_OFFSET_IN_FLASH

CONFIG_BOOTLOADER_OFFSET_IN_FLASH=0x0
bootloader的烧录偏移地址
.bin包在./build/bootloader
CONFIG_APP_BUILD_TYPE_APP_2NDBOOT
CONFIG_APP_BUILD_TYPE_APP_2NDBOOT=y
使用二级引导启动，也即是bootloader启动，一级引导是出厂烧死在芯片内部的程序

分区表
名称
定义
说明
使用(如有)
CONFIG_PARTITION_TABLE_OFFSET
CONFIG_PARTITION_TABLE_OFFSET=0xC000
分区表偏移地址
.bin包在./build/partition_table
日志
名称
定义
说明
使用(如有)
CONFIG_LOG_DEFAULT_LEVEL
CONFIG_LOG_DEFAULT_LEVEL=3
日志等级

日志等级
暂时无法在飞书文档外展示此内容
看门狗
名称
定义
说明
使用(如有)
CONFIG_ESP_INT_WDT
CONFIG_ESP_INT_WDT=y
开启、关闭 中断看门狗

CONFIG_ESP_INT_WDT_TIMEOUT_MS
CONFIG_ESP_INT_WDT_TIMEOUT_MS=3000
中断看门狗 3秒超时

CONFIG_ESP_TASK_WDT_EN
CONFIG_ESP_TASK_WDT_EN=y
开启、关闭 任务看门狗

CONFIG_ESP_TASK_WDT_TIMEOUT_S
CONFIG_ESP_TASK_WDT_TIMEOUT_S=30
任务看门狗 30s 超时

系统
名称
定义
说明
使用(如有)
CONFIG_ESP_DEFAULT_CPU_FREQ_MHZ
CONFIG_ESP_DEFAULT_CPU_FREQ_MHZ=160
主频160MHz，即1秒有160000000个周期

波特率
名称
定义
说明
使用(如有)
CONFIG_ESP_CONSOLE_UART_BAUDRATE/CONFIG_CONSOLE_UART_BAUDRATE
CONFIG_ESP_CONSOLE_UART_BAUDRATE=921600
串口日志波特率

FLASH加密
名称
定义
说明
使用(如有)
CONFIG_SECURE_FLASH_ENC_ENABLED
CONFIG_SECURE_FLASH_ENC_ENABLED=y
启动FLASH加密

CONFIG_NVS_ENCRYPTION
CONFIG_NVS_ENCRYPTION=y
启动NVS加密

CONFIG_NVS_SEC_KEY_PROTECT_USING_FLASH_ENC
CONFIG_NVS_SEC_KEY_PROTECT_USING_FLASH_ENC=y
使用FLASH Encryption机制保护NVS的密钥
与下面取一
CONFIG_NVS_SEC_KEY_PROTECT_USING_HMAC
CONFIG_NVS_SEC_KEY_PROTECT_USING_HMAC=y
使用HMAC保护NVS密钥
与上面取一
安全启动
名称
定义
说明
使用(如有)
CONFIG_SECURE_BOOT
CONFIG_SECURE_BOOT=y
安全启动

